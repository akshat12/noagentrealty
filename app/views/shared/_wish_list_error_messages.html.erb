<% if @wish_list.errors.any? %>
    <div class="alert alert-error">
      <strong> The form contains <%= pluralize(@wish_list.errors.count, "error") %>: </strong>
    
    <ul>
    <% @wish_list.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
   
  </div>
<% end %>
