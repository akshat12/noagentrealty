<% if @flag.errors.any? %>
    <div class="alert alert-error">
      <strong> The form contains <%= pluralize(@flag.errors.count, "error") %>: </strong>
    
    <ul>
    <% @flag.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
   
  </div>
<% end %>
